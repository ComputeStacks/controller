nl:
  volumes:
    backups:
      new: Create a Backup
      restore:
        title_html: '<i class="fa fa-refresh"></i> Restore'
        confirm: Are you sure? This will wipe out the current data set and restart the associated containers.
      name: Backup Name
