de:
  container_services:
    setup:
      domains:
        title: Standard Domain
        ip: Öffentliche IP Adresse
      ingress:

        # This is the section header for the remote connection snippet
        # e.g. `mysql -u root -h <ip> -p` for mysql.
        title: Befehl beim verbinden
