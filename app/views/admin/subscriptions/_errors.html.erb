<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@subscription.errors.count, "error")} prevented this subscription from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @subscription.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
