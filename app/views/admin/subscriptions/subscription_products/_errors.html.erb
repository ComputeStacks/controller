<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@subscription_product.errors.count, "error")} prevented this subscription product from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @subscription_product.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
