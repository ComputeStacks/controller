<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@load_balancer.errors.count, "error")} prevented this load balancer from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @load_balancer.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>