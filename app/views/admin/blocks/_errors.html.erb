<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@block.errors.count, "error")} prevented this content block from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @block.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
