<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@region.errors.count, "error")} prevented this availability zone from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @region.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
