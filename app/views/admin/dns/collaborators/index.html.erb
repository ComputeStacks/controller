<% unless request.xhr? %>
  <% content_for :breadcrumb do %>
    <div class="row has-breadcrumb">
      <ol class="breadcrumb">
        <li><%= link_to t('dns.index.breadcrumb'), "/admin/dns" %></li>
        <li><%= link_to @zone.name, "/admin/dns/#{@zone.id}" %></li>
        <li class="active">Collaborators</li>
      </ol>
    </div>
  <% end %>
<% end %>

<%= render partial: 'collaborators/index', locals: { resource: @zone, collaborators: @collaborators, base_path: "/admin/dns/#{@zone.id}/collaborators" } %>

