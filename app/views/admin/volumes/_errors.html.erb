<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@volume.errors.count, "error")} prevented this volume from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @volume.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
