<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@location.errors.count, "error")} prevented this region from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @location.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
