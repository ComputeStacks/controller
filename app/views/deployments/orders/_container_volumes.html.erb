<div class="panel-body">
  <div class="row">
    <div class="col-xs-12"><strong>VOLUMES <a style="cursor:pointer;" class="volume-param-toggle" data-list-id="vol-param-list-<%= container[:container_id] %>">[SHOW]</a></strong></div>
  </div>
  <div class="volume-param-list" id="vol-param-list-<%= container[:container_id] %>" style="display:none;">
    <div class="row">
      <div class="col-sm-6 col-xs-12">
        <div class="code-font-minimal">
          <% container[:volumes].each do |volume| %>
            <% if volume[:source].blank? %>
              <%= volume[:source].blank? ? "Create Volume #{volume[:label]} and mount at " : "Mount Volume #{volume[:source]} at " %><%= volume[:mount_path] %>
            <% else %>
              <%= volume[:source] =~ /project/ ? "Clone " : "Mount " %><%= "Volume #{volume[:source]} at " %><%= volume[:mount_path] %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
<hr>
