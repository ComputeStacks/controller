<div class="panel panel-default">
  <div class="panel-body">
    <p class="text-center">Memory Throttled Events</p>
    <%= area_chart "#{container_charts_memory_throttled_path(container.id)}.json",
                   points: false,
                   download: true,
                   refresh: 60,
                   label: 'Events',
                   curve: false,
                   legend: false
    %>
  </div>
</div>
