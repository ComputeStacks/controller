<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(errors.count, "error")} prevented this #{obj} from being saved:" %></strong>
    <ul class="list-unstyled">
      <% errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
