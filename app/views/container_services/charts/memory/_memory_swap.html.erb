<div class="panel panel-default">
  <div class="panel-body">
    <p class="text-center">Swap (MB)</p>
    <%= area_chart "#{container_service_charts_memory_swap_path(service.id)}.json",
                   points: false,
                   download: true,
                   refresh: 60,
                   label: 'MB',
                   curve: false,
                   legend: service.containers.count > 6 ? false : 'bottom'
    %>
  </div>
</div>
