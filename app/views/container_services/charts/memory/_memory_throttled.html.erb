<div class="panel panel-default">
  <div class="panel-body">
    <p class="text-center">Memory Throttled Events</p>
    <%= area_chart "#{container_service_charts_memory_throttled_path(service.id)}.json",
                   points: false,
                   download: true,
                   refresh: 60,
                   label: 'Events',
                   curve: false,
                   legend: service.containers.count > 6 ? false : 'bottom'
    %>
  </div>
</div>
