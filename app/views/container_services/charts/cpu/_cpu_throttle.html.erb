<div class="panel panel-default">
  <div class="panel-body">
    <p class="text-center">CPU Throttle Events (Seconds)</p>
    <%= area_chart "#{container_service_charts_cpu_throttled_path(service.id)}.json",
                   points: false,
                   download: true,
                   refresh: 60,
                   label: "Seconds",
                   curve: false,
                   legend: service.containers.count > 6 ? false : 'bottom'
    %>
  </div>
</div>
