<div class="col-sm-4">
  <div class="panel panel-body">
    <strong><%= "#{pluralize(@service.errors.count, "error")} prevented this service from being saved:" %></strong>
    <ul class="list-unstyled">
      <% @service.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
