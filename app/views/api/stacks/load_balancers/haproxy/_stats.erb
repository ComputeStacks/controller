frontend stats
  bind <%= load_balancer.stats_bind %>
  mode http
  http-request use-service prometheus-exporter if { path /metrics }
  stats enable
  stats uri /stats
  stats refresh 10s
  stats auth admin:<%= load_balancer.stats_password %>
